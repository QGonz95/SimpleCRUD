<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title></title>
</head>

<body>
    <h1>Client Lists</h1>
    <ul>
        <% for(let i=0; i < clients.length; i++){ %>
            <li>
                <a href="/clients/<%=clients[i].id;%>"> Name: <%=clients[i].name %>, Goal: <%= clients[i].goal %>,
                            Sessions: <%= clients[i].sessions%>  
                            Payment: <%= clients[i].payment%>
                            </a>
                            <a href="/clients/<%=clients[i].id;%>/edit">Edit</a>
                            <form action="/clients/<%=clients[i].id; %>?_method=DELETE" method="POST">
                                <input type="submit" value="x"/>
                            </form>                        
            </li>
            <% } %>
    </ul>
    <nav>
        <a href="/clients/new">Add Client</a>
    </nav>
</body>

</html>